/*
import DLibX.*;
import java.awt.*;
import java.util.*;

public Class FindColor extends Minigame {

  //Instance Variables
  DConsole dc;
  private boolean game;
  //To keep track of time
  int cycles;
  int seconds; 

  //Constructor
  public FindColor(int id, DConsole dc, ArrayList<Player> playerList) {
    super(id, dc, playerList);
    this.dc = dc;
  }
  
  //Entities
  private void addEntites(){
    
  }

  //Spawn Players
  private void spawnPlayers() {
    playerList.get(0).setPOS(250, 180);
    playerList.get(1).setPOS(350, 180);
    playerList.get(2).setPOS(250, 360);
    playerList.get(3).setPOS(350, 360);
  }

  









  
}
*/